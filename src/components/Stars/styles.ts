import styled from 'styled-components'

export const StarsSection = styled.div`
    display: flex;
    align-items: center;
    gap: 0.2rem;

    span {
        margin-left: 0.4rem;
        color: #fff;
    }
    
`